import { ARView } from 'react-native-arkit';

const ARGarden = () => {
  return (
    <ARView style={{ flex: 1 }}>
      {/* AR elements like virtual plants can be added here */}
    </ARView>
  );
};

export default ARGarden;
